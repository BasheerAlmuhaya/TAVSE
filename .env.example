# ─────────────────────────────────────────────────────────────
# TAVSE: Local Environment Configuration
# ─────────────────────────────────────────────────────────────
# Copy this file to .env and edit for your local setup:
#   cp .env.example .env
#
# All paths below are REQUIRED. The project will not run without them.
# These values are NEVER committed to git (see .gitignore).
# ─────────────────────────────────────────────────────────────

# ── Storage Paths ─────────────────────────────────────────────
# Root directory for all TAVSE data (processed LMDBs, audio, noise, etc.)
# Should point to a high-capacity, fast-access filesystem.
# Examples:
#   HPC scratch:  /mnt/scratch/users/<YOUR_ID>/tavse
#   Local SSD:    /data/tavse
#   NAS mount:    /mnt/nas/tavse
TAVSE_DATA_ROOT=/path/to/your/tavse/data

# Root directory for the TAVSE source code
# Usually the directory containing this .env file.
TAVSE_PROJECT_DIR=/path/to/TAVSE

# ── HuggingFace ───────────────────────────────────────────────
# Where to cache HuggingFace downloads.
# IMPORTANT: Do NOT use your home directory if it has a quota.
# Set to a location with ample space (>50 GB recommended).
HF_HOME=${TAVSE_DATA_ROOT}/.hf_cache

# ── SLURM / HPC (optional, only for cluster users) ───────────
# Partition name for GPU jobs (default: gpu)
# SLURM_GPU_PARTITION=gpu
# Partition name for CPU-only jobs (check: sinfo -o "%P")
# SLURM_CPU_PARTITION=nodes
# GPU resource request (default: gpu:1)
# SLURM_GPU_GRES=gpu:1
# Conda environment name (default: tavse)
# TAVSE_CONDA_ENV=tavse

# ── GPU Configuration (optional) ─────────────────────────────
# Set to "true" to enable TF32 on Ampere+ GPUs (A100, RTX 30xx, etc.)
# TF32 gives ~2x speedup on matmul/conv with negligible accuracy loss.
# TAVSE_ENABLE_TF32=true

# Set to "true" to use BF16 instead of FP16 for mixed precision.
# BF16 is more numerically stable and natively supported on A100s.
# TAVSE_USE_BF16=true

# Set to "true" to enable torch.compile() for PyTorch 2.x graph optimization.
# TAVSE_TORCH_COMPILE=false
